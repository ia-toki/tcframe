coverage:
  parsers:
    gcov:
      branch_detection:
        macro: no
        loop: no
        conditional: no
        method: no
  ignore:
    - ete-test/.*
    - include/tcframe/deprecated/.*
    - test/.*
    - usr/include/.*
